{% extends 'destination.html' %}
{% block destination_content %}	

<div style="text-align:center" id="destination_heading">
	<h2 class="blue-text text-darken-4 search_text_font z-depth-2" id="heading">{{destination.title}}</h2>
</div>

<div class="slider" id="slider_div">
    <ul class="slides">
	
	{% for p in destimages %}      


	<li>
        <img class="slider_images" src="{{ p.image.url }}"> <!-- random image -->
      </li>
	{% endfor %}
  	</ul>
  </div>

<div class="row">

      <div class="col s4 m4 l3 z-depth-2 hide-on-med-and-down">
   
	<ul class="collapsible" data-collapsible="accordion">
	{% if waystoreach %}
    <li>
      <div class="collapsible-header"><a href="#" class="black-text"><i class="material-icons">airplanemode_active</i>Way to reach</a></div>
    </li>
	{% endif %}


	{% if attractions %}
    <li>
	<div class="collapsible-header"><i class="material-icons">place</i>Attractions</div>
	<div class="collapsible-body">
	<div class="collection">
	{% for a in attractions %}
        <a href="#!" class="collection-item">{{a.title}}</a>
        {% endfor %}
	</div>
	</div>
    </li>
	{% endif %}
    {% if things %}
    <li>
      <div class="collapsible-header"><a href="{% url 'destination-things' %}" class="black-text"><i class="material-icons">playlist_add_check</i>Things to do</a></div>
    </li>
	{% endif %}
  </ul>
      </div>

      <div class="col s12 m12 l9 z-depth-2">
        <!-- Teal page content  -->
      </div>

    </div>





{% block scripts %}
	
<script>


$('.slider').slider({full_width: true, height: 400,interval:4000});
</script>


{% endblock %}



{% endblock %}
